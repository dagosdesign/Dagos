# Used only by `npm run build:native` (vite --mode native): the phone app's server.
# Public values; the secret keys never go here. Change both when the app gets its own domain.
VITE_API_BASE="https://lex-zmt3.onrender.com"
VITE_ASSET_BASE="https://lex-zmt3.onrender.com"
